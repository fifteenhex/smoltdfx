/*
 * GL/glu.h - stub.  GLQuake includes <GL/glu.h> but on this backend uses
 * its own MYgluPerspective and #if 0's the gluBuild2DMipmaps/gluScaleImage
 * paths, so nothing from GLU is actually needed.
 */
#ifndef __smolminigl_glu_h
#define __smolminigl_glu_h
#include "gl.h"
#endif
